import IconTables from './classes/icon-tables';
import type { ColorMode } from './types';
export declare const db: IconTables;
export interface GetClassOptions {
    /**
     * The color mode to use for the icon.
     *
     * @defaultValue 'light'
     */
    colorMode?: ColorMode;
    /**
     * Whether or no the icon returned should be for a directory.
     *
     * @defaultValue false
     */
    isDir?: boolean;
    /**
     * If passed, no fallback icon will be returned
     *
     * @defaultValue false
     */
    skipFallback?: boolean;
}
/**
 * Get a joined icon class name of the provided filename. If not found, default to text icon.
 *
 * @param name - File name
 * @returns A joined class string to apply to the icon's element
 */
export declare function getIconClass(name: string, { colorMode, isDir, skipFallback, }?: GetClassOptions): string | null;
/**
 * Get an array of icon classese for the provided filename. If not found, default to text icon.
 *
 * @param name - File name
 * @returns A list of classes to apply to the icon's element
 */
export declare function getIconClassList(name: string, { colorMode, isDir, skipFallback }?: GetClassOptions): string[] | null;
