export declare function normalizeManifestPermissions(manifest?: chrome.runtime.Manifest): Required<chrome.permissions.Permissions>;
type OriginsOptions = {
    strictOrigins?: boolean;
};
type AdditionalPermissionsOptions = OriginsOptions & {
    manifest?: chrome.runtime.Manifest;
};
export declare function queryAdditionalPermissions(options?: OriginsOptions): Promise<Required<chrome.permissions.Permissions>>;
export declare function extractAdditionalPermissions(currentPermissions: chrome.permissions.Permissions, { manifest, strictOrigins, }?: AdditionalPermissionsOptions): Required<chrome.permissions.Permissions>;
export declare function isUrlPermittedByManifest(origin: string, manifest?: chrome.runtime.Manifest): boolean;
export declare function dropOverlappingPermissions({ origins, permissions }: chrome.permissions.Permissions): Required<chrome.permissions.Permissions>;
export {};
